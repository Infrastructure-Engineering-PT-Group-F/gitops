---
# Runtime-secret delivery for the Argo CD-owned validation tenant.
# All payloads remain in Google Secret Manager. This manifest contains only
# source names, target contracts, and non-secret registry metadata.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: api-keys
  namespace: tenant-validation
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: api-keys
    creationPolicy: Owner
    template:
      engineVersion: v2
      mergePolicy: Merge
      type: Opaque
  data:
    - secretKey: avwx-api-key
      remoteRef:
        key: avwx-api-key
---
# Workload image-pull Secret for the private GHCR backend and frontend images.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-pull
  namespace: tenant-validation
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: ghcr-pull
    creationPolicy: Owner
    template:
      engineVersion: v2
      mergePolicy: Replace
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: >-
          {{ printf "{\"auths\":{\"ghcr.io\":{\"username\":\"%s\",\"password\":\"%s\",\"auth\":\"%s\"}}}" "hochschule-jz" .token (printf "%s:%s" "hochschule-jz" .token | b64enc) }}
  data:
    - secretKey: token
      remoteRef:
        key: ghcr-pull
---
# Helm provider chart-pull Secret for private OCI Helm charts in GHCR.
# It uses the same externally managed GHCR token as ghcr-pull, but the
# Crossplane Helm provider requires discrete username and password keys.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-chart-pull
  namespace: tenant-validation
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: ghcr-chart-pull
    creationPolicy: Owner
    template:
      engineVersion: v2
      mergePolicy: Replace
      type: Opaque
      data:
        username: hochschule-jz
        password: "{{ .token }}"
  data:
    - secretKey: token
      remoteRef:
        key: ghcr-pull
